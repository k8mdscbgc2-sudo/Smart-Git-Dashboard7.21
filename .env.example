# Smart Git Dashboard — Frontend env

# BACKEND_API_BASE_URL：Next.js 服务端读取，用于把 /api/v1/* 反代到后端。
# 这是 **服务端私有 env**（没有 NEXT_PUBLIC_ 前缀，不会暴露到客户端 bundle）。
# 浏览器永远访问同源 /api/v1，零 CORS。
#
# Vercel：Project → Settings → Environment Variables 添加
# 本地：复制本文件为 .env.local，填你自己的值
#
# Railway 后端（默认）：
BACKEND_API_BASE_URL=https://smart-commit-helper-backend-production.up.railway.app
# 本地后端：
# BACKEND_API_BASE_URL=http://localhost:8000